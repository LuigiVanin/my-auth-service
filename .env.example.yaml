# Server Configuration
# Server Configuration
server:
  port: 8080

# App Configuration
app:
  env: development
  name: AUTH SERVICE
  encryption_key: Should Be Random UUID

# Database Configuration
database:
  host: localhost
  port: 5432
  user: postgres
  password: password
  name: auth_db
  sslmode: disable

email:
  resend_api_key: your_resend_api_key
